## MODIFIED Requirements

### Requirement: Shadow User auto-creation
When a `crm_client_profile` with a phone number is saved and no linked `user_id` exists, the system SHALL look up the site `users` table by phone. If found, the profile SHALL be linked to that user. If not found, a new site `users` record SHALL be created with `status = 'SHADOW'` (not `account_type`) and a UUID generated by the CRM backend (not the database), and the profile linked to it.

#### Scenario: Phone matches existing site user
- **WHEN** a CRM client is saved with a phone that exists in `users.phone`
- **THEN** `crm_client_profiles.user_id` is set to the matching user's `id` (UUID string); no new user is created

#### Scenario: Phone has no site user — Shadow User created
- **WHEN** a CRM client is saved with a phone that does not exist in `users.phone`
- **THEN** a new `users` record is inserted with `id = uuid4()`, `phone`, `status = 'SHADOW'`, `created_at = now()`, `updated_at = now()`; `crm_client_profiles.user_id` is set to it

#### Scenario: Client saved without phone
- **WHEN** a CRM client profile is created without a phone number
- **THEN** no Shadow User lookup or creation occurs; `user_id` remains null

#### Scenario: Concurrent phone insert handled
- **WHEN** two concurrent requests attempt to create a Shadow User for the same phone
- **THEN** only one `users` row is inserted (IntegrityError caught); both requests link to the same user

## REMOVED Requirements

### Requirement: Client profile free-text notes field
**Reason**: The `notes` field on `crm_client_profiles` is replaced by the dedicated `crm_client_notes` table introduced in `crm-client-notes` capability. Structured notes support authorship, edit history, work order linkage, and soft delete — capabilities the free-text field cannot provide.
**Migration**: Existing non-empty `notes` values are migrated as a single initial `crm_client_notes` entry per profile during Phase 2.3 deployment. After migration, the `notes` column on `crm_client_profiles` is deprecated and will be dropped in a follow-up migration once confirmed stable.
